<h1> <%= @patient.name %> </h1> Appointments:

<% @patient.appointments.each do |appt| %>
   Time: <%= appt.appointment_datetime.strftime("%B %e, %Y at %H:%M") %> </br>
   Doctor: <%= link_to appt.doctor.name, doctor_path(appt.doctor)%>
    <%end%>